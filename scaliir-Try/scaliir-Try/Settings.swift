//
//  Settings.swift
//  scaliir-Try
//
//  Created by Marian Nasturica on 17.08.2021.
//
/*
import Foundation
import Combine

class UserSettings: ObservableObject {
    @Published var sizeX: Int {
        didSet {
            UserDefaults.standard.set(sizeX, forKey: "Rows")
        }
    }
    @Published var sizeY: Int {
        didSet {
            UserDefaults.standard.set(sizeY, forKey: "Columns")
        }
    }
    init() {
        self.sizeX = UserDefaults.standard.object(forKey: "Rows") as? Int ?? 32
        self.sizeY = UserDefaults.standard.object(forKey: "Columns") as? Int ?? 32
    }
}
*/
